{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build:types",
			"type": "shell",
			"command": "pnpm -w -s tsc -p tsconfig.json --noEmit",
			"problemMatcher": ["$tsc"],
			"group": "build"
		},
		{
			"label": "typecheck:full (root) run",
			"type": "shell",
			"command": "pnpm -w -s tsc -p tsconfig.json --noEmit",
			"group": "build"
		}
	]
}
